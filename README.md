# AI-based-compiler-optimization
ML-based recommendation system that predicts the optimal compiler optimization sequence for a C-like language (MiniC), validated against real compiled-binary speedups rather than a static cost model.
